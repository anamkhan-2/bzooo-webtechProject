<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - Zoo Store</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .confirmation-container {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f8f9fa;
        }

        .confirmation-card {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
            padding: 3rem;
            max-width: 600px;
            width: 100%;
            margin: 2rem;
        }

        .success-icon {
            width: 80px;
            height: 80px;
            background-color: #2ecc71;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            font-size: 3rem;
            color: white;
        }

        .confirmation-title {
            text-align: center;
            color: #212529;
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .confirmation-subtitle {
            text-align: center;
            color: #6c757d;
            font-size: 1.1rem;
            margin-bottom: 2rem;
        }

        .order-details {
            background-color: #f8f9fa;
            padding: 2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid #e9ecef;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            color: #6c757d;
            font-weight: 500;
        }

        .detail-value {
            color: #212529;
            font-weight: 600;
        }

        .order-id {
            background-color: #e7f5f0;
            border: 2px solid #2ecc71;
            padding: 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            text-align: center;
        }

        .order-id-label {
            color: #6c757d;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .order-id-value {
            color: #2ecc71;
            font-size: 1.5rem;
            font-weight: 700;
            font-family: 'Courier New', monospace;
        }

        .items-list {
            margin-bottom: 2rem;
        }

        .item-row {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid #e9ecef;
        }

        .item-name {
            flex: 1;
            font-weight: 500;
            color: #212529;
        }

        .item-qty {
            color: #6c757d;
            font-size: 0.9rem;
            margin: 0 1rem;
        }

        .item-price {
            color: #2ecc71;
            font-weight: 600;
            min-width: 80px;
            text-align: right;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .action-buttons button {
            flex: 1;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-home {
            background-color: #6c757d;
            color: white;
            border: none;
        }

        .btn-home:hover {
            background-color: #5a6268;
        }

        .btn-orders {
            background-color: #2ecc71;
            color: white;
            border: none;
        }

        .btn-orders:hover {
            background-color: #27ae60;
        }

        .status-badge {
            display: inline-block;
            background-color: #d1ecf1;
            color: #0c5460;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
        }
    </style>
</head>

<body>
    <div class="confirmation-container">
        <div class="confirmation-card">
            <!-- Success Icon -->
            <div class="success-icon">✓</div>

            <!-- Title and Subtitle -->
            <h1 class="confirmation-title">Order Confirmed!</h1>
            <p class="confirmation-subtitle">Thank you for your purchase. Your order has been successfully placed.</p>

            <!-- Order ID -->
            <div class="order-id">
                <div class="order-id-label">Order ID</div>
                <div class="order-id-value"><%= order._id %></div>
            </div>

            <!-- Order Details -->
            <div class="order-details">
                <div class="detail-row">
                    <span class="detail-label">Customer Name:</span>
                    <span class="detail-value"><%= order.customerName %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Email:</span>
                    <span class="detail-value"><%= order.email %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Status:</span>
                    <span class="detail-value">
                        <span class="status-badge"><%= order.status %></span>
                    </span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Payment Method:</span>
                    <span class="detail-value"><%= order.paymentMethod.toUpperCase() %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Order Date:</span>
                    <span class="detail-value"><%= new Date(order.createdDate).toLocaleDateString() %></span>
                </div>
            </div>

            <!-- Items List -->
            <h3 style="color: #212529; margin-bottom: 1rem; font-weight: 600;">Order Items</h3>
            <div class="items-list">
                <% order.cartItems.forEach(item => { %>
                    <div class="item-row">
                        <span class="item-name"><%= item.product %></span>
                        <span class="item-qty">x<%= item.quantity %></span>
                        <span class="item-price">$<%= item.subtotal.toFixed(2) %></span>
                    </div>
                <% }) %>
                <div style="margin-top: 1.5rem; padding-top: 1rem; border-top: 2px solid #e9ecef;">
                    <div class="item-row">
                        <span class="item-name">Subtotal</span>
                        <span class="item-price">$<%= order.subtotal.toFixed(2) %></span>
                    </div>
                    <div class="item-row">
                        <span class="item-name">Shipping</span>
                        <span class="item-price">$<%= order.shipping.toFixed(2) %></span>
                    </div>
                    <div class="item-row">
                        <span class="item-name">Tax</span>
                        <span class="item-price">$<%= order.tax.toFixed(2) %></span>
                    </div>
                    <div class="item-row" style="border: none; margin-top: 1rem; font-size: 1.2rem;">
                        <span class="item-name" style="font-weight: 700;">Total</span>
                        <span class="item-price" style="font-size: 1.2rem;">$<%= order.totalAmount.toFixed(2) %></span>
                    </div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="action-buttons">
                <button type="button" class="btn-home" onclick="window.location.href='/'">Back to Home</button>
                <button type="button" class="btn-orders" onclick="window.location.href='/api/orders'">View All Orders</button>
            </div>

            <!-- Additional Info -->
            <div style="text-align: center; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid #e9ecef; color: #6c757d; font-size: 0.9rem;">
                <p>A confirmation email has been sent to <strong><%= order.email %></strong></p>
                <p>You can track your order using Order ID: <strong><%= order._id %></strong></p>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
